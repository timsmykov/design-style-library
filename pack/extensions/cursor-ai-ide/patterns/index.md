# Patterns

Reusable runtime patterns for `cursor-ai-ide`.

Style formula: AI code editor workspace; dark IDE shell; chat/composer sidecar; files, diffs, terminal and model controls as first-class surfaces; practical developer speed over decoration

## Selection rule

Pick patterns by semantic job first, visual similarity second. Use the corresponding capsule when implementing.

## AI Chat Panel

Capsule: `components/capsules/ai-chat-panel.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Composer Command Card

Capsule: `components/capsules/composer-command-card.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Diff Review Pane

Capsule: `components/capsules/diff-review-pane.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Editor Shell

Capsule: `components/capsules/editor-shell.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## File Explorer

Capsule: `components/capsules/file-explorer.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Model Selector

Capsule: `components/capsules/model-selector.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Onboarding Import Project

Capsule: `components/capsules/onboarding-import-project.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Pricing Plan Grid

Capsule: `components/capsules/pricing-plan-grid.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Settings Preferences

Capsule: `components/capsules/settings-preferences.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Terminal Panel

Capsule: `components/capsules/terminal-panel.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Cross-pattern rules

- Navigation and shell patterns should establish orientation before content density increases.
- Data/status patterns should make state legible before showing secondary metadata.
- Marketing/pricing patterns should keep proof, action, and comparison cleanly separated.
- Settings/admin patterns should privilege scanability, defaults, and clear destructive-action treatment.
