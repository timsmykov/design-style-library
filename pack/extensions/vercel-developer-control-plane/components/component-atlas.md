# Component atlas

Runtime component atlas for `vercel-developer-control-plane`.

Use this file to choose the right capsule before implementation. The capsule is the detailed recipe; this atlas is the router.

## `analytics-chart` — Vercel Developer Control Plane — Analytics Chart

Use when: the artifact needs `analytics chart` behavior, layout, or decision structure.
Capsule: `components/capsules/analytics-chart.md`
Semantic slice: `pack/components/vercel-developer-control-plane/analytics-chart.md`
Evidence links recorded: `2` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Analytics Chart Use this capsule when the artifact needs `analytics chart` behavior in the Vercel Developer Control Plane style. ## Grammar - Style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof - Evidence refs: 6 local images. - Token anchors: canvas `#000000`, surface `#0a0a0a`, text `#fafafa`, accent `#ffffff`. - Preserve information hierarchy and action semantics;

## `deployment-list` — Vercel Developer Control Plane — Deployment List

Use when: the artifact needs `deployment list` behavior, layout, or decision structure.
Capsule: `components/capsules/deployment-list.md`
Semantic slice: `pack/components/vercel-developer-control-plane/deployment-list.md`
Evidence links recorded: `2` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Deployment List Use this capsule when the artifact needs `deployment list` behavior in the Vercel Developer Control Plane style. ## Grammar - Style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof - Evidence refs: 6 local images. - Token anchors: canvas `#000000`, surface `#0a0a0a`, text `#fafafa`, accent `#ffffff`. - Preserve information hierarchy and action semantics;

## `docs-code-block` — Vercel Developer Control Plane — Docs Code Block

Use when: the artifact needs `docs code block` behavior, layout, or decision structure.
Capsule: `components/capsules/docs-code-block.md`
Semantic slice: `pack/components/vercel-developer-control-plane/docs-code-block.md`
Evidence links recorded: `2` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Docs Code Block Use this capsule when the artifact needs `docs code block` behavior in the Vercel Developer Control Plane style. ## Grammar - Style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof - Evidence refs: 6 local images. - Token anchors: canvas `#000000`, surface `#0a0a0a`, text `#fafafa`, accent `#ffffff`. - Preserve information hierarchy and action semantics;

## `domain-settings` — Vercel Developer Control Plane — Domain Settings

Use when: the artifact needs `domain settings` behavior, layout, or decision structure.
Capsule: `components/capsules/domain-settings.md`
Semantic slice: `pack/components/vercel-developer-control-plane/domain-settings.md`
Evidence links recorded: `2` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Domain Settings Use this capsule when the artifact needs `domain settings` behavior in the Vercel Developer Control Plane style. ## Grammar - Style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof - Evidence refs: 6 local images. - Token anchors: canvas `#000000`, surface `#0a0a0a`, text `#fafafa`, accent `#ffffff`. - Preserve information hierarchy and action semantics;

## `environment-variable-table` — Vercel Developer Control Plane — Environment Variable Table

Use when: the artifact needs `environment variable table` behavior, layout, or decision structure.
Capsule: `components/capsules/environment-variable-table.md`
Semantic slice: `pack/components/vercel-developer-control-plane/environment-variable-table.md`
Evidence links recorded: `2` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Environment Variable Table Use this capsule when the artifact needs `environment variable table` behavior in the Vercel Developer Control Plane style. ## Grammar - Style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof - Evidence refs: 6 local images. - Token anchors: canvas `#000000`, surface `#0a0a0a`, text `#fafafa`, accent `#ffffff`. - Preserve information hierarchy

## `import-project-flow` — Vercel Developer Control Plane — Import Project Flow

Use when: the artifact needs `import project flow` behavior, layout, or decision structure.
Capsule: `components/capsules/import-project-flow.md`
Semantic slice: `pack/components/vercel-developer-control-plane/import-project-flow.md`
Evidence links recorded: `2` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Import Project Flow Use this capsule when the artifact needs `import project flow` behavior in the Vercel Developer Control Plane style. ## Grammar - Style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof - Evidence refs: 6 local images. - Token anchors: canvas `#000000`, surface `#0a0a0a`, text `#fafafa`, accent `#ffffff`. - Preserve information hierarchy and action se

## `logs-console` — Vercel Developer Control Plane — Logs Console

Use when: the artifact needs `logs console` behavior, layout, or decision structure.
Capsule: `components/capsules/logs-console.md`
Semantic slice: `pack/components/vercel-developer-control-plane/logs-console.md`
Evidence links recorded: `2` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Logs Console Use this capsule when the artifact needs `logs console` behavior in the Vercel Developer Control Plane style. ## Grammar - Style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof - Evidence refs: 6 local images. - Token anchors: canvas `#000000`, surface `#0a0a0a`, text `#fafafa`, accent `#ffffff`. - Preserve information hierarchy and action semantics; adapt

## `pricing-plan-grid` — Vercel Developer Control Plane — Pricing Plan Grid

Use when: the artifact needs `pricing plan grid` behavior, layout, or decision structure.
Capsule: `components/capsules/pricing-plan-grid.md`
Semantic slice: `pack/components/vercel-developer-control-plane/pricing-plan-grid.md`
Evidence links recorded: `2` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Pricing Plan Grid Use this capsule when the artifact needs `pricing plan grid` behavior in the Vercel Developer Control Plane style. ## Grammar - Style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof - Evidence refs: 6 local images. - Token anchors: canvas `#000000`, surface `#0a0a0a`, text `#fafafa`, accent `#ffffff`. - Preserve information hierarchy and action semant

## `project-card` — Vercel Developer Control Plane — Project Card

Use when: the artifact needs `project card` behavior, layout, or decision structure.
Capsule: `components/capsules/project-card.md`
Semantic slice: `pack/components/vercel-developer-control-plane/project-card.md`
Evidence links recorded: `2` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Project Card Use this capsule when the artifact needs `project card` behavior in the Vercel Developer Control Plane style. ## Grammar - Style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof - Evidence refs: 6 local images. - Token anchors: canvas `#000000`, surface `#0a0a0a`, text `#fafafa`, accent `#ffffff`. - Preserve information hierarchy and action semantics; adapt

## `template-gallery` — Vercel Developer Control Plane — Template Gallery

Use when: the artifact needs `template gallery` behavior, layout, or decision structure.
Capsule: `components/capsules/template-gallery.md`
Semantic slice: `pack/components/vercel-developer-control-plane/template-gallery.md`
Evidence links recorded: `2` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Template Gallery Use this capsule when the artifact needs `template gallery` behavior in the Vercel Developer Control Plane style. ## Grammar - Style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof - Evidence refs: 6 local images. - Token anchors: canvas `#000000`, surface `#0a0a0a`, text `#fafafa`, accent `#ffffff`. - Preserve information hierarchy and action semantic
