# Patterns

Reusable runtime patterns for `perplexity-answer-engine`.

Style formula: Evidence-backed Perplexity Answer Engine style grammar derived from local tokens, component capsules, and captured visual corpus.

## Selection rule

Pick patterns by semantic job first, visual similarity second. Use the corresponding capsule when implementing.

## Answer Results Thread

Capsule: `components/capsules/answer-results-thread.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## App Shell Sidebar

Capsule: `components/capsules/app-shell-sidebar.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Collections Library

Capsule: `components/capsules/collections-library.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Follow Up Related Questions

Capsule: `components/capsules/follow-up-related-questions.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Help Article Layout

Capsule: `components/capsules/help-article-layout.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Onboarding Auth

Capsule: `components/capsules/onboarding-auth.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Pro Upgrade Pricing

Capsule: `components/capsules/pro-upgrade-pricing.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Public Landing Pricing

Capsule: `components/capsules/public-landing-pricing.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Search Composer

Capsule: `components/capsules/search-composer.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Settings Account

Capsule: `components/capsules/settings-account.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Sources Citation Strip

Capsule: `components/capsules/sources-citation-strip.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Cross-pattern rules

- Navigation and shell patterns should establish orientation before content density increases.
- Data/status patterns should make state legible before showing secondary metadata.
- Marketing/pricing patterns should keep proof, action, and comparison cleanly separated.
- Settings/admin patterns should privilege scanability, defaults, and clear destructive-action treatment.
