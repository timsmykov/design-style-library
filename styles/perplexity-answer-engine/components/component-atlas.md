# Component atlas

Runtime component atlas for `perplexity-answer-engine`.

Use this file to choose the right capsule before implementation. The capsule is the detailed recipe; this atlas is the router.

## `answer-results-thread` — Answer Results Thread

Use when: the artifact needs `answer results thread` behavior, layout, or decision structure.
Capsule: `components/capsules/answer-results-thread.md`
Semantic slice: `pack/components/perplexity-answer-engine/answer-results-thread.md`
Evidence links recorded: `3` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: Evidence-backed Perplexity Answer Engine style grammar derived from local tokens, component capsules, and captured visual corpus.
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Answer Results Thread ## Use when Use this component for answer citations sources follow-up related question thread result in an answer-first, source-backed interface. ## Structure ```text Component ├── concise label/title ├── content or control body ├── evidence/context metadata └── compact action row ``` ## Implementation recipe - Use white or cool-off-white surfaces with thin neutral borders. - Keep typography crisp and utility-sized around the answer body. - Use teal only for active/action

## `app-shell-sidebar` — App Shell Sidebar

Use when: the artifact needs `app shell sidebar` behavior, layout, or decision structure.
Capsule: `components/capsules/app-shell-sidebar.md`
Semantic slice: `pack/components/perplexity-answer-engine/app-shell-sidebar.md`
Evidence links recorded: `3` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: Evidence-backed Perplexity Answer Engine style grammar derived from local tokens, component capsules, and captured visual corpus.
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # App Shell Sidebar ## Use when Use this component for home discover library spaces collections threads sidebar in an answer-first, source-backed interface. ## Structure ```text Component ├── concise label/title ├── content or control body ├── evidence/context metadata └── compact action row ``` ## Implementation recipe - Use white or cool-off-white surfaces with thin neutral borders. - Keep typography crisp and utility-sized around the answer body. - Use teal only for active/action/proof accent

## `collections-library` — Collections Library

Use when: the artifact needs `collections library` behavior, layout, or decision structure.
Capsule: `components/capsules/collections-library.md`
Semantic slice: `pack/components/perplexity-answer-engine/collections-library.md`
Evidence links recorded: `3` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: Evidence-backed Perplexity Answer Engine style grammar derived from local tokens, component capsules, and captured visual corpus.
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Collections Library ## Use when Use this component for collections library spaces saved threads in an answer-first, source-backed interface. ## Structure ```text Component ├── concise label/title ├── content or control body ├── evidence/context metadata └── compact action row ``` ## Implementation recipe - Use white or cool-off-white surfaces with thin neutral borders. - Keep typography crisp and utility-sized around the answer body. - Use teal only for active/action/proof accents. - Preserve 

## `follow-up-related-questions` — Follow Up Related Questions

Use when: the artifact needs `follow up related questions` behavior, layout, or decision structure.
Capsule: `components/capsules/follow-up-related-questions.md`
Semantic slice: `pack/components/perplexity-answer-engine/follow-up-related-questions.md`
Evidence links recorded: `3` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: Evidence-backed Perplexity Answer Engine style grammar derived from local tokens, component capsules, and captured visual corpus.
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Follow Up Related Questions ## Use when Use this component for related follow up question chips cards in an answer-first, source-backed interface. ## Structure ```text Component ├── concise label/title ├── content or control body ├── evidence/context metadata └── compact action row ``` ## Implementation recipe - Use white or cool-off-white surfaces with thin neutral borders. - Keep typography crisp and utility-sized around the answer body. - Use teal only for active/action/proof accents. - Pre

## `help-article-layout` — Help Article Layout

Use when: the artifact needs `help article layout` behavior, layout, or decision structure.
Capsule: `components/capsules/help-article-layout.md`
Semantic slice: `pack/components/perplexity-answer-engine/help-article-layout.md`
Evidence links recorded: `3` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: Evidence-backed Perplexity Answer Engine style grammar derived from local tokens, component capsules, and captured visual corpus.
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Help Article Layout ## Use when Use this component for faq help article detail support in an answer-first, source-backed interface. ## Structure ```text Component ├── concise label/title ├── content or control body ├── evidence/context metadata └── compact action row ``` ## Implementation recipe - Use white or cool-off-white surfaces with thin neutral borders. - Keep typography crisp and utility-sized around the answer body. - Use teal only for active/action/proof accents. - Preserve traceabil

## `onboarding-auth` — Onboarding Auth

Use when: the artifact needs `onboarding auth` behavior, layout, or decision structure.
Capsule: `components/capsules/onboarding-auth.md`
Semantic slice: `pack/components/perplexity-answer-engine/onboarding-auth.md`
Evidence links recorded: `3` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: Evidence-backed Perplexity Answer Engine style grammar derived from local tokens, component capsules, and captured visual corpus.
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Onboarding Auth ## Use when Use this component for signup login verify account onboarding upload download in an answer-first, source-backed interface. ## Structure ```text Component ├── concise label/title ├── content or control body ├── evidence/context metadata └── compact action row ``` ## Implementation recipe - Use white or cool-off-white surfaces with thin neutral borders. - Keep typography crisp and utility-sized around the answer body. - Use teal only for active/action/proof accents. -

## `pro-upgrade-pricing` — Pro Upgrade Pricing

Use when: the artifact needs `pro upgrade pricing` behavior, layout, or decision structure.
Capsule: `components/capsules/pro-upgrade-pricing.md`
Semantic slice: `pack/components/perplexity-answer-engine/pro-upgrade-pricing.md`
Evidence links recorded: `3` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: Evidence-backed Perplexity Answer Engine style grammar derived from local tokens, component capsules, and captured visual corpus.
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Pro Upgrade Pricing ## Use when Use this component for pro upgrade subscribe plan billing checkout in an answer-first, source-backed interface. ## Structure ```text Component ├── concise label/title ├── content or control body ├── evidence/context metadata └── compact action row ``` ## Implementation recipe - Use white or cool-off-white surfaces with thin neutral borders. - Keep typography crisp and utility-sized around the answer body. - Use teal only for active/action/proof accents. - Preser

## `public-landing-pricing` — Public Landing Pricing

Use when: the artifact needs `public landing pricing` behavior, layout, or decision structure.
Capsule: `components/capsules/public-landing-pricing.md`
Semantic slice: `pack/components/perplexity-answer-engine/public-landing-pricing.md`
Evidence links recorded: `3` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: Evidence-backed Perplexity Answer Engine style grammar derived from local tokens, component capsules, and captured visual corpus.
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Public Landing Pricing ## Use when Use this component for landing hero pricing product trust pro public in an answer-first, source-backed interface. ## Structure ```text Component ├── concise label/title ├── content or control body ├── evidence/context metadata └── compact action row ``` ## Implementation recipe - Use white or cool-off-white surfaces with thin neutral borders. - Keep typography crisp and utility-sized around the answer body. - Use teal only for active/action/proof accents. - P

## `search-composer` — Search Composer

Use when: the artifact needs `search composer` behavior, layout, or decision structure.
Capsule: `components/capsules/search-composer.md`
Semantic slice: `pack/components/perplexity-answer-engine/search-composer.md`
Evidence links recorded: `3` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: Evidence-backed Perplexity Answer Engine style grammar derived from local tokens, component capsules, and captured visual corpus.
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Search Composer ## Use when Use this component for ask anything search composer focus attach model in an answer-first, source-backed interface. ## Structure ```text Component ├── concise label/title ├── content or control body ├── evidence/context metadata └── compact action row ``` ## Implementation recipe - Use white or cool-off-white surfaces with thin neutral borders. - Keep typography crisp and utility-sized around the answer body. - Use teal only for active/action/proof accents. - Preser

## `settings-account` — Settings Account

Use when: the artifact needs `settings account` behavior, layout, or decision structure.
Capsule: `components/capsules/settings-account.md`
Semantic slice: `pack/components/perplexity-answer-engine/settings-account.md`
Evidence links recorded: `3` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: Evidence-backed Perplexity Answer Engine style grammar derived from local tokens, component capsules, and captured visual corpus.
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Settings Account ## Use when Use this component for settings profile account preferences controls in an answer-first, source-backed interface. ## Structure ```text Component ├── concise label/title ├── content or control body ├── evidence/context metadata └── compact action row ``` ## Implementation recipe - Use white or cool-off-white surfaces with thin neutral borders. - Keep typography crisp and utility-sized around the answer body. - Use teal only for active/action/proof accents. - Preserv

## `sources-citation-strip` — Sources Citation Strip

Use when: the artifact needs `sources citation strip` behavior, layout, or decision structure.
Capsule: `components/capsules/sources-citation-strip.md`
Semantic slice: `pack/components/perplexity-answer-engine/sources-citation-strip.md`
Evidence links recorded: `3` index path groups; see local source map and extracted folders for detail.

Design job:
- Preserve this style formula: Evidence-backed Perplexity Answer Engine style grammar derived from local tokens, component capsules, and captured visual corpus.
- Keep the component's primary object obvious before adding decoration.
- Use token roles and density from `tokens/tokens.json` instead of inventing ad-hoc values.
- Adapt the grammar to the new product context; do not clone exact evidence screens.

Implementation notes:
- Start with semantic structure and states.
- Add controls, status, metadata, and supporting copy in that order.
- Check responsive behavior and empty/error/loading states when relevant.

Evidence excerpt:
> # Sources Citation Strip ## Use when Use this component for sources citations numbered references cards in an answer-first, source-backed interface. ## Structure ```text Component ├── concise label/title ├── content or control body ├── evidence/context metadata └── compact action row ``` ## Implementation recipe - Use white or cool-off-white surfaces with thin neutral borders. - Keep typography crisp and utility-sized around the answer body. - Use teal only for active/action/proof accents. - Pre
