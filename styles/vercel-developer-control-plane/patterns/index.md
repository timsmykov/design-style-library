# Patterns

Reusable runtime patterns for `vercel-developer-control-plane`.

Style formula: black/white developer command surface; sharp grids; deployment status as primary object; mono technical detail; premium gradients used sparingly behind product proof

## Selection rule

Pick patterns by semantic job first, visual similarity second. Use the corresponding capsule when implementing.

## Analytics Chart

Capsule: `components/capsules/analytics-chart.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Deployment List

Capsule: `components/capsules/deployment-list.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Docs Code Block

Capsule: `components/capsules/docs-code-block.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Domain Settings

Capsule: `components/capsules/domain-settings.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Environment Variable Table

Capsule: `components/capsules/environment-variable-table.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Import Project Flow

Capsule: `components/capsules/import-project-flow.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Logs Console

Capsule: `components/capsules/logs-console.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Pricing Plan Grid

Capsule: `components/capsules/pricing-plan-grid.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Project Card

Capsule: `components/capsules/project-card.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Template Gallery

Capsule: `components/capsules/template-gallery.md`

Use when the artifact needs this job-to-be-done, not merely when the reference image looks similar.
Keep hierarchy, state semantics, density, and token roles aligned with the capsule. Adapt content and layout to the user's product context.

## Cross-pattern rules

- Navigation and shell patterns should establish orientation before content density increases.
- Data/status patterns should make state legible before showing secondary metadata.
- Marketing/pricing patterns should keep proof, action, and comparison cleanly separated.
- Settings/admin patterns should privilege scanability, defaults, and clear destructive-action treatment.
